import * as fetch from 'node-fetch'

export class GarantexApi {
    cosntructor(credentials) {}

    async fetchExchangeRates(): Promise<{btc_rub: number, eth_rub: number, usdt_rub: number}> {
        return null;
    }
}
